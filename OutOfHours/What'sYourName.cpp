#include <iostream>
using namespace std;
int main() {
    
    // display question to screen
    cout << "How old are you: ";
    // Declare a varian;e
    int age;
    // Initialise the value of the age variable with the character input >>
    cin >> age;
    // Display and print to the console the concatonated sentence with ~ age = variable
    cout << "I am " << age << " years old.";
   
}
